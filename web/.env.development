# URL del backend FastAPI en desarrollo.
# Si vas a probar desde el celular en la misma red WiFi, cambia 127.0.0.1 por la IP de tu PC
# (ej. http://192.168.1.20:8000) y agrega ese origen a CORS_ORIGINS en el .env del backend.
VITE_API_URL=http://127.0.0.1:8000
